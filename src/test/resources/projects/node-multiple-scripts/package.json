{
  "name": "test-seed4j-project",
  "version": "0.0.0",
  "private": true,
  "description": "Seed4J Project",
  "license": "UNLICENSED",
  "scripts": {
    "build": "vue-tsc -p tsconfig-build.json --noEmit && vite build --emptyOutDir",
    "serve": "vue-tsc -p tsconfig-build.json --noEmit && vite build --emptyOutDir"
  },
  "dependencies": {
    "@playwright/test": "6.1.1"
  },
  "devDependencies": {
    "@prettier/plugin-xml": "2.1.0"
  },
  "engines": {
    "node": ">=16.13.0"
  }
}
