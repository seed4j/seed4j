package {{ packageName }}.shared.dependencies.domain;

import com.seed4j.module.domain.nodejs.NodePackagesVersionSource;
import com.seed4j.module.domain.nodejs.NodePackagesVersionSourceFactory;

public enum {{ baseName }}NodePackagesVersionSource implements NodePackagesVersionSourceFactory {
  {{ baseNameUpperCased }}("{{ baseNameKebabCased }}");

  private final String source;

  {{ baseName }}NodePackagesVersionSource(String source) {
    this.source = source;
  }

  @Override
  public NodePackagesVersionSource build() {
    return new NodePackagesVersionSource(source);
  }
}
