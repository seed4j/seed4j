import { User } from '@/common/domain/User';

export interface UserDTO {
  login: string;
  authorities: string[];
}

export const toUser = (userDTO: UserDTO): User => ({
  username: userDTO.login,
  authorities: userDTO.authorities,
});
