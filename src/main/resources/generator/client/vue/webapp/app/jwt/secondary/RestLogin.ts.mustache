import { Login } from '@/common/domain/Login';

export interface RestLogin {
  username: string;
  password: string;
  rememberMe: boolean;
}

export const toRestLogin = (login: Login): RestLogin => ({
  username: login.username,
  password: login.password,
  rememberMe: login.rememberMe,
});
