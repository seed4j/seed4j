import { Login } from '@/common/domain/Login';

export interface LoginDTO {
  username: string;
  password: string;
  rememberMe: boolean;
}

export const toLoginDTO = (login: Login): LoginDTO => ({
  username: login.username,
  password: login.password,
  rememberMe: login.rememberMe,
});
